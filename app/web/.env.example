# Leave this unset for local Vite development: `/api` is proxied to the local
# Gateway so the browser can keep its HttpOnly session cookie. Set it only for
# a separately hosted gateway. Never put secrets in this public Vite file.
VITE_GATEWAY_URL=
